namespace Fabric.Terminology.API.Constants
{
    using Catalyst.DosApi.Discovery.Configuration;

    /// <summary>
    /// Dictionary keys used in DiscoveryService configuration.  See <see cref="DiscoveryServiceClientSettings"/> in appsettings.json.
    /// </summary>
    public static class DiscoveryServicesKeys
    {
        public const string Identity = "Identity";

        public const string Authorization = "Authorization";
    }
}