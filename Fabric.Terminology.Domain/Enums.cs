namespace Fabric.Terminology.Domain
{
    public enum SortDirection
    {
        Ascending,
        Descending
    }

    public enum ValueSetStatus
    {
        Draft = 1,
        Active = 0,
        Archived = 2
    }
}