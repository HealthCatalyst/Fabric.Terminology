namespace Fabric.Terminology.Domain
{
    public enum SortDirection
    {
        Ascending,
        Descending
    }

    public enum ValueSetStatusCode
    {
        Draft,
        Active,
        Archived
    }
}